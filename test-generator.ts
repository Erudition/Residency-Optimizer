import { generateSchedule, getUnifiedResidents } from './services/scheduler';
import fs from 'fs';

// To run this, I need valid programData and residents...
// It's probably easier to just capture the state from the app!
